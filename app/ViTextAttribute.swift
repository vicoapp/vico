import Foundation

/**
 * Names of text attributes for use in NSAttributedStrings, and specifically in
 * the editor's backing text storage.
 */
enum ViTextAttribute: String {
    case Folded = "ViFoldedAttribute"
}